"use client";

export default function LandingAmbient() {
  return (
    <div
      className="fixed inset-0 pointer-events-none overflow-hidden z-0 bg-[var(--bg-primary)]"
      aria-hidden
    />
  );
}